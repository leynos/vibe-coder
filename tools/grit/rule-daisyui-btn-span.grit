`<span className=$class>$body</span>` as $node where {
  $class <: contains "btn",
  register_diagnostic(span=$class, message="[daisyui/semantic-btn] Apply DaisyUI 'btn' to a <button> or <a>, not a <span>.")
}
