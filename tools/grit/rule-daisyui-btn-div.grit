`<div className=$class>$body</div>` as $node where {
  $class <: contains "btn",
  register_diagnostic(span=$class, message="[daisyui/semantic-btn] Apply DaisyUI 'btn' to a <button> or <a>, not a <div>.")
}
