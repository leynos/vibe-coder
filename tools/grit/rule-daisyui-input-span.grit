`<span className=$class>$body</span>` as $node where {
  $class <: contains "input",
  register_diagnostic(span=$class, message="[daisyui/semantic-input] Apply DaisyUI 'input' to an <input>, not a <span>.")
}
