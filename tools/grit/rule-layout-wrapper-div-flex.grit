`<div className="flex $rest" $attrs>$body</div>` as $node where {
  register_diagnostic(span=$node, message="Layout-only wrapper detected. Extract a named layout class such as `.stack` or `.cluster`.")
}
