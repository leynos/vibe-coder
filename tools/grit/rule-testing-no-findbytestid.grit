`$recv.$method($arg)` as $node where {
  $method <: `findByTestId`,
  register_diagnostic($method, message="[test/no-testid-selectors] Prefer accessible queries (findByRole/Label/Placeholder/Alt/Title). Using *ByTestId requires an ACCESSIBILITY justification.")
}
