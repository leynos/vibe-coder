/**
 * @file Domain model barrel.
 *
 * The domain model layer holds immutable value objects and aggregate roots used
 * by `src/domain/services/` and `src/domain/rules/`. Files placed here must not
 * import React, Dexie, Web Audio, browser APIs, application code, or adapter
 * code. See `docs/vibe-coder-high-level-design.md` section "Module layout".
 */

