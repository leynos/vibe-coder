/**
 * @file Domain layer barrel.
 *
 * The domain layer contains pure business logic: value objects, entities,
 * domain services, and port interfaces. Modules in this layer must not import
 * React, Dexie, Canvas, Web Audio, browser storage, or service-worker APIs.
 *
 * See `docs/adr-002-adopt-hexagonal-architecture-for-domain-boundaries.md`
 * for the full rationale and boundary rules.
 */
