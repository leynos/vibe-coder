/**
 * @file Domain ports barrel.
 *
 * Domain ports define the interfaces the domain requires from external systems.
 * Adapters implement these interfaces; domain code must not import adapter
 * implementations directly. See `docs/vibe-coder-high-level-design.md` section
 * "Module layout".
 */

