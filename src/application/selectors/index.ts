/**
 * @file Application selectors barrel.
 *
 * Application selectors expose read-only projections for UI and workflow code.
 * They may depend on domain types, but must not reach into adapters or the app
 * shell. See `docs/vibe-coder-high-level-design.md` section "Module layout".
 */

